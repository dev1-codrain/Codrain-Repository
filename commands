git config --global user.email "dev1.codraintech2gmail.com"
git config --global user.name "dev1 codrain"

learn git part-1:

1. git init
2.git add [filename] || -A
3.git status
4.git commit -m "msg"
5.git remote add origin repoName
6.git push origin master
7.git clone repoNAme
8. git pull origin master